apiVersion: v1
kind: Namespace
metadata:
  name: hivemind
---
apiVersion: v1
kind: Secret
metadata:
  name: hivemind-env
  namespace: hivemind
type: Opaque
stringData:
  FLASK_DEBUG: "false"
  HOST: "0.0.0.0"
  PORT: "5000"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hivemind-coordinator
  namespace: hivemind
  labels:
    app.kubernetes.io/name: hivemind
    app.kubernetes.io/part-of: shsw-dev
    app.kubernetes.io/component: ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hivemind-coordinator
  template:
    metadata:
      labels:
        app: hivemind-coordinator
        app.kubernetes.io/name: hivemind
        app.kubernetes.io/part-of: shsw-dev
    spec:
      securityContext:
        fsGroup: 1001
      containers:
        - name: coordinator
          image: 127.0.0.1:5000/hivemind-coordinator:latest
          imagePullPolicy: Always
          env:
            - name: FLASK_DEBUG
              valueFrom:
                secretKeyRef:
                  name: hivemind-env
                  key: FLASK_DEBUG
            - name: HOST
              valueFrom:
                secretKeyRef:
                  name: hivemind-env
                  key: HOST
            - name: PORT
              valueFrom:
                secretKeyRef:
                  name: hivemind-env
                  key: PORT
          ports:
            - name: http
              containerPort: 5000
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 1001
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hivemind-cloudflared-token
                  key: token
          args:
            - tunnel
            - --no-autoupdate
            - --url
            - http://localhost:5000
            - run
            - --token
            - $(TUNNEL_TOKEN)
---
apiVersion: v1
kind: Service
metadata:
  name: hivemind-coordinator
  namespace: hivemind
  labels:
    app.kubernetes.io/name: hivemind
    app.kubernetes.io/part-of: shsw-dev
spec:
  selector:
    app: hivemind-coordinator
  ports:
    - name: http
      port: 5000
      targetPort: http
