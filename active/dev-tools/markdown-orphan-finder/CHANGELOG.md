# Changelog

## 0.2.0 (2026-06-11)

- Add `--version` flag to print version and exit
- Add `--json` flag for machine-readable JSON output
- Add `pyproject.toml` with `find-orphans` console_scripts entry point
- Update README with usage examples for new flags

## 0.1.0 (Initial)

- Initial release with orphan detection and island finding
- Support for `--exclude`, `--show-links` flags
- Convenience script `run_on_megarepo.sh`
