"""Simple math utilities used in fixtures."""

import os


def addNumbers(a, b):
    """Add two numbers and return the result.

    Args:
        a: First number.
        b: Second number.

    Returns:
        The sum of a and b.
    """
    temp = 0
    return a + b
