apiVersion: v1
kind: Namespace
metadata:
  name: ops
---
apiVersion: v1
kind: Secret
metadata:
  name: ops-env
  namespace: ops
type: Opaque
stringData:
  FLASK_DEBUG: "false"
  HOST: "0.0.0.0"
  PORT: "5000"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ops-data
  namespace: ops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ops
  namespace: ops
  labels:
    app.kubernetes.io/name: ops
    app.kubernetes.io/part-of: shsw-dev
    app.kubernetes.io/component: devtools
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ops
  template:
    metadata:
      labels:
        app: ops
        app.kubernetes.io/name: ops
        app.kubernetes.io/part-of: shsw-dev
    spec:
      securityContext:
        fsGroup: 1001
      initContainers:
        - name: prepare-app-data
          image: busybox:1.36
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-ec"]
          args:
            - |
              mkdir -p /app/data
              chown -R 1001:1001 /app/data
              chmod -R ug+rwX /app/data
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: ops-data
              mountPath: /app/data
      containers:
        - name: ops
          image: 127.0.0.1:5000/ops:latest
          imagePullPolicy: Always
          env:
            - name: FLASK_DEBUG
              valueFrom:
                secretKeyRef:
                  name: ops-env
                  key: FLASK_DEBUG
            - name: HOST
              valueFrom:
                secretKeyRef:
                  name: ops-env
                  key: HOST
            - name: PORT
              valueFrom:
                secretKeyRef:
                  name: ops-env
                  key: PORT
          ports:
            - name: http
              containerPort: 5000
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 1001
          volumeMounts:
            - name: ops-data
              mountPath: /app/data
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: ops-cloudflared-token
                  key: token
          args:
            - tunnel
            - --no-autoupdate
            - --url
            - http://localhost:5000
            - run
            - --token
            - $(TUNNEL_TOKEN)
      volumes:
        - name: ops-data
          persistentVolumeClaim:
            claimName: ops-data
---
apiVersion: v1
kind: Service
metadata:
  name: ops
  namespace: ops
  labels:
    app.kubernetes.io/name: ops
    app.kubernetes.io/part-of: shsw-dev
spec:
  selector:
    app: ops
  ports:
    - name: http
      port: 5000
      targetPort: http
