export type AST = any;

export interface InterpreterOptions {
  export class Interpreter {
    constructor() {
      throw new Error('Material primitives are no longer supported; tag system only.');
    }
  }
  ops = 0;
